# Spade LinkedIn ads, production set

Rendered preview of the 25 ads cleared to design, for the ads team.

Open: https://spade-data-inc-labs.github.io/ads-preview/

Temporary. This repo is public only because GitHub Pages requires it on the free plan, and
it carries `noindex` plus a `robots.txt` disallow so it should not be listed by search engines.
Delete the repo when the ads are built.
